# Database
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=freelance_manager

# API
JWT_SECRET=generate-a-strong-random-string-of-at-least-32-characters
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Web (build-time — baked into Next.js bundle)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Stripe (optional)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_PRICE_ID=
STRIPE_ENTERPRISE_PRICE_ID=

# eNotas (optional)
ENOTAS_API_KEY=
ENOTAS_WEBHOOK_SECRET=
